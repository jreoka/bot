# Rust build output and run artifacts.
/target/
/checkpoints/

# The parity fixtures in parity/out/ are committed on purpose: they are the
# oracle the port is verified against, and a test that regenerates its own
# oracle can drift with it. See NOTES.md.
